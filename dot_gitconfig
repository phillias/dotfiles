# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = phillias
	email = phillias@phillias.us
[credential "https://github.com"]
	helper = 
	helper = !/bin/sh -c 'for p in /usr/local/bin/gh /usr/bin/gh; do [ -x "$p" ] && exec "$p" auth git-credential "$@"; done' _
[credential "https://gist.github.com"]
	helper = 
	helper = !/bin/sh -c 'for p in /usr/local/bin/gh /usr/bin/gh; do [ -x "$p" ] && exec "$p" auth git-credential "$@"; done' _
[credential]
	helper = store
