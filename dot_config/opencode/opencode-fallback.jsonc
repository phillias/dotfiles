{
  // PAID-FIRST fallback policy (captain decision 2026-08-12, DESIGN.md §2.6).
  // 2026-09-01 (captain decision, decision A v5): All opencode chains re-pointed
  // to the GLM-5.1 ladder — big-pickle → OpenCode Go glm-5.1 → GOAT GLM-5.2 →
  // OpenRouter glm-5 → OpenCode Zen glm-5.1. PGS, Cloudflare Workers, the
  // Z.AI Coding Plan, and the openrouter :free trio are retired from the
  // interactive ladders (PGS burns Coding Plan credits; CF @cf excluded by
  // captain; Z.AI / free trio are GATE-chain-only via private_dot_pi
  // fallback-chains.json). go-pool-guard.ts retired 2026-08-12 — reactive chain
  // stepping here is the single owner of exhaustion handling.
  //
  // Fleet taxonomy (the retired OmO agent keys are gone): the global ladder is
  // firstmate's + every secondmate's session chain (no agent name → resolves
  // here). `agents` covers crewmate subagent types + the ce-* persona swarm.
  // `categories` covers firstmate's dispatch categories. Resolution order:
  // session model → agent (exact, then longest `*` wildcard) → category →
  // global ladder. Specialized entries set `no_global_tail` so the chain ends
  // at Zen and FAILS visibly instead of silently downgrading to the free tier.
  "enabled": true,
  "retry_on_errors": [400, 401, 402, 403, 429, 500, 502, 503, 504, 529],
  "max_fallback_attempts": 15,
  "cooldown_seconds": 60,
  "timeout_seconds": 120,
  "notify_on_fallback": true,

  // GLOBAL LADDER — firstmate & secondmate main sessions (agent=undefined),
  // and the deduped tail of every non-no_global_tail chain.
  "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],

  // CREWMATES — subagent types firstmate dispatches via task(subagent_type=...).
  "agents": {
    // Utility crewmates: big-pickle primary → Go → GOAT → OR → Zen (GLM-5.1 ladder).
    "general": {
      "model": "opencode-zen/big-pickle",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ]
    },
    "explore": {
      "model": "opencode-zen/big-pickle",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ]
    },
    // Specialized flywheel agents: models pinned, fallback runs the GLM-5.1 ladder.
    "self-improve": {
      "model": "opencode-zen/glm-5.1",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    },
    "solutions-research": {
      "model": "opencode-zen/nemotron-3-ultra-free",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    },
    // ce-* review/research personas: models stay pinned in their agent .md
    // files. On error fall back through the GLM-5.1 ladder; chain end surfaces
    // as a visible failure (no free downgrade via global tail).
    "ce-*": {
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    }
  },

  // DISPATCH CATEGORIES — firstmate picks at the intent gate via
  // dispatch-rules.json → task(category=...).
  "categories": {
    // UTILITY (decision A v5): GLM-5.1 ladder below.
    "quick": {
      "model": "opencode-zen/big-pickle",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ]
    },
    "unspecified-low": {
      "model": "opencode-zen/big-pickle",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ]
    },
    // HIGH-INTENSITY / SPECIALIZED (decision A): models pinned, fallback runs
    // the GLM-5.1 ladder; no free downgrade via global tail — exhaustion fails loudly.
    "ultrabrain": {
      "model": "opencode-go/deepseek-v4-pro",
      "variant": "xhigh",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    },
    "deep": {
      "model": "opencode-zen/big-pickle",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    },
    "unspecified-high": {
      "model": "opencode-zen/big-pickle",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    },
    "visual-engineering": {
      "model": "opencode/gpt-5.3-codex",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    },
    "artistry": {
      "model": "opencode-zen/gemini-3.5-flash",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    },
    "writing": {
      "model": "opencode-zen/deepseek-v4-flash-free",
      "fallback_models": [
        "opencode-zen/big-pickle",
        // OpenCode Go ($60 pool) - GLM-5.1
        "opencode-go/glm-5.1",
        // Command Code GOAT ($70 pool) - lowest GLM-5.x mainline on GOAT
        "commandcode/zai-org/GLM-5.2",
        // OpenRouter GLM-5 (cheapest per-token mainline, $0.60/$1.92)
        "openrouter/z-ai/glm-5",
        // OpenCode Zen GLM-5.1 (128k)
        "opencode-zen/glm-5.1"
      ],
      "no_global_tail": true
    }
  }
}
