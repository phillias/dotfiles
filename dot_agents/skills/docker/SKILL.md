# Docker Skill

This skill has moved to `~/docker/selfhost/.opencode/skills/docker/SKILL.md`.

The docker skill was consolidated into the selfhost repo during the June 2026
locket simplification (see `.omo/plans/selfhost-migration-simplify.md`).

Load it by path when working with selfhost infrastructure.
